// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"database/sql"
	"fmt"
	"reflect"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
	"gopkg.in/volatiletech/null.v6"
)

// UpepGeneIdentifier is an object representing the database table.
type UpepGeneIdentifier struct {
	ID        int64     `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt null.Time `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	Gi        int64     `boil:"gi" json:"gi" toml:"gi" yaml:"gi"`

	R *upepGeneIdentifierR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L upepGeneIdentifierL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UpepGeneIdentifierColumns = struct {
	ID        string
	CreatedAt string
	UpdatedAt string
	Gi        string
}{
	ID:        "id",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
	Gi:        "gi",
}

// upepGeneIdentifierR is where relationships are stored.
type upepGeneIdentifierR struct {
	GiUpepRefSeqEntries UpepRefSeqEntrySlice
}

// upepGeneIdentifierL is where Load methods for each relationship are stored.
type upepGeneIdentifierL struct{}

var (
	upepGeneIdentifierColumns               = []string{"id", "created_at", "updated_at", "gi"}
	upepGeneIdentifierColumnsWithoutDefault = []string{"created_at", "updated_at", "gi"}
	upepGeneIdentifierColumnsWithDefault    = []string{"id"}
	upepGeneIdentifierPrimaryKeyColumns     = []string{"id"}
)

type (
	// UpepGeneIdentifierSlice is an alias for a slice of pointers to UpepGeneIdentifier.
	// This should generally be used opposed to []UpepGeneIdentifier.
	UpepGeneIdentifierSlice []*UpepGeneIdentifier
	// UpepGeneIdentifierHook is the signature for custom UpepGeneIdentifier hook methods
	UpepGeneIdentifierHook func(boil.Executor, *UpepGeneIdentifier) error

	upepGeneIdentifierQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	upepGeneIdentifierType                 = reflect.TypeOf(&UpepGeneIdentifier{})
	upepGeneIdentifierMapping              = queries.MakeStructMapping(upepGeneIdentifierType)
	upepGeneIdentifierPrimaryKeyMapping, _ = queries.BindMapping(upepGeneIdentifierType, upepGeneIdentifierMapping, upepGeneIdentifierPrimaryKeyColumns)
	upepGeneIdentifierInsertCacheMut       sync.RWMutex
	upepGeneIdentifierInsertCache          = make(map[string]insertCache)
	upepGeneIdentifierUpdateCacheMut       sync.RWMutex
	upepGeneIdentifierUpdateCache          = make(map[string]updateCache)
	upepGeneIdentifierUpsertCacheMut       sync.RWMutex
	upepGeneIdentifierUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force bytes in case of primary key column that uses []byte (for relationship compares)
	_ = bytes.MinRead
)
var upepGeneIdentifierBeforeInsertHooks []UpepGeneIdentifierHook
var upepGeneIdentifierBeforeUpdateHooks []UpepGeneIdentifierHook
var upepGeneIdentifierBeforeDeleteHooks []UpepGeneIdentifierHook
var upepGeneIdentifierBeforeUpsertHooks []UpepGeneIdentifierHook

var upepGeneIdentifierAfterInsertHooks []UpepGeneIdentifierHook
var upepGeneIdentifierAfterSelectHooks []UpepGeneIdentifierHook
var upepGeneIdentifierAfterUpdateHooks []UpepGeneIdentifierHook
var upepGeneIdentifierAfterDeleteHooks []UpepGeneIdentifierHook
var upepGeneIdentifierAfterUpsertHooks []UpepGeneIdentifierHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UpepGeneIdentifier) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UpepGeneIdentifier) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UpepGeneIdentifier) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UpepGeneIdentifier) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UpepGeneIdentifier) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UpepGeneIdentifier) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UpepGeneIdentifier) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UpepGeneIdentifier) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UpepGeneIdentifier) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range upepGeneIdentifierAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUpepGeneIdentifierHook registers your hook function for all future operations.
func AddUpepGeneIdentifierHook(hookPoint boil.HookPoint, upepGeneIdentifierHook UpepGeneIdentifierHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		upepGeneIdentifierBeforeInsertHooks = append(upepGeneIdentifierBeforeInsertHooks, upepGeneIdentifierHook)
	case boil.BeforeUpdateHook:
		upepGeneIdentifierBeforeUpdateHooks = append(upepGeneIdentifierBeforeUpdateHooks, upepGeneIdentifierHook)
	case boil.BeforeDeleteHook:
		upepGeneIdentifierBeforeDeleteHooks = append(upepGeneIdentifierBeforeDeleteHooks, upepGeneIdentifierHook)
	case boil.BeforeUpsertHook:
		upepGeneIdentifierBeforeUpsertHooks = append(upepGeneIdentifierBeforeUpsertHooks, upepGeneIdentifierHook)
	case boil.AfterInsertHook:
		upepGeneIdentifierAfterInsertHooks = append(upepGeneIdentifierAfterInsertHooks, upepGeneIdentifierHook)
	case boil.AfterSelectHook:
		upepGeneIdentifierAfterSelectHooks = append(upepGeneIdentifierAfterSelectHooks, upepGeneIdentifierHook)
	case boil.AfterUpdateHook:
		upepGeneIdentifierAfterUpdateHooks = append(upepGeneIdentifierAfterUpdateHooks, upepGeneIdentifierHook)
	case boil.AfterDeleteHook:
		upepGeneIdentifierAfterDeleteHooks = append(upepGeneIdentifierAfterDeleteHooks, upepGeneIdentifierHook)
	case boil.AfterUpsertHook:
		upepGeneIdentifierAfterUpsertHooks = append(upepGeneIdentifierAfterUpsertHooks, upepGeneIdentifierHook)
	}
}

// OneP returns a single upepGeneIdentifier record from the query, and panics on error.
func (q upepGeneIdentifierQuery) OneP() *UpepGeneIdentifier {
	o, err := q.One()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single upepGeneIdentifier record from the query.
func (q upepGeneIdentifierQuery) One() (*UpepGeneIdentifier, error) {
	o := &UpepGeneIdentifier{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for upep_gene_identifiers")
	}

	if err := o.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
		return o, err
	}

	return o, nil
}

// AllP returns all UpepGeneIdentifier records from the query, and panics on error.
func (q upepGeneIdentifierQuery) AllP() UpepGeneIdentifierSlice {
	o, err := q.All()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all UpepGeneIdentifier records from the query.
func (q upepGeneIdentifierQuery) All() (UpepGeneIdentifierSlice, error) {
	var o []*UpepGeneIdentifier

	err := q.Bind(&o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UpepGeneIdentifier slice")
	}

	if len(upepGeneIdentifierAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(queries.GetExecutor(q.Query)); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountP returns the count of all UpepGeneIdentifier records in the query, and panics on error.
func (q upepGeneIdentifierQuery) CountP() int64 {
	c, err := q.Count()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all UpepGeneIdentifier records in the query.
func (q upepGeneIdentifierQuery) Count() (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count upep_gene_identifiers rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table, and panics on error.
func (q upepGeneIdentifierQuery) ExistsP() bool {
	e, err := q.Exists()
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q upepGeneIdentifierQuery) Exists() (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow().Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if upep_gene_identifiers exists")
	}

	return count > 0, nil
}

// GiUpepRefSeqEntriesG retrieves all the upep_ref_seq_entry's upep ref seq entries via gi_id column.
func (o *UpepGeneIdentifier) GiUpepRefSeqEntriesG(mods ...qm.QueryMod) upepRefSeqEntryQuery {
	return o.GiUpepRefSeqEntries(boil.GetDB(), mods...)
}

// GiUpepRefSeqEntries retrieves all the upep_ref_seq_entry's upep ref seq entries with an executor via gi_id column.
func (o *UpepGeneIdentifier) GiUpepRefSeqEntries(exec boil.Executor, mods ...qm.QueryMod) upepRefSeqEntryQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"upep\".\"upep_ref_seq_entries\".\"gi_id\"=?", o.ID),
	)

	query := UpepRefSeqEntries(exec, queryMods...)
	queries.SetFrom(query.Query, "\"upep\".\"upep_ref_seq_entries\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"upep\".\"upep_ref_seq_entries\".*"})
	}

	return query
}

// LoadGiUpepRefSeqEntries allows an eager lookup of values, cached into the
// loaded structs of the objects.
func (upepGeneIdentifierL) LoadGiUpepRefSeqEntries(e boil.Executor, singular bool, maybeUpepGeneIdentifier interface{}) error {
	var slice []*UpepGeneIdentifier
	var object *UpepGeneIdentifier

	count := 1
	if singular {
		object = maybeUpepGeneIdentifier.(*UpepGeneIdentifier)
	} else {
		slice = *maybeUpepGeneIdentifier.(*[]*UpepGeneIdentifier)
		count = len(slice)
	}

	args := make([]interface{}, count)
	if singular {
		if object.R == nil {
			object.R = &upepGeneIdentifierR{}
		}
		args[0] = object.ID
	} else {
		for i, obj := range slice {
			if obj.R == nil {
				obj.R = &upepGeneIdentifierR{}
			}
			args[i] = obj.ID
		}
	}

	query := fmt.Sprintf(
		"select * from \"upep\".\"upep_ref_seq_entries\" where \"gi_id\" in (%s)",
		strmangle.Placeholders(dialect.IndexPlaceholders, count, 1, 1),
	)
	if boil.DebugMode {
		fmt.Fprintf(boil.DebugWriter, "%s\n%v\n", query, args)
	}

	results, err := e.Query(query, args...)
	if err != nil {
		return errors.Wrap(err, "failed to eager load upep_ref_seq_entries")
	}
	defer results.Close()

	var resultSlice []*UpepRefSeqEntry
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice upep_ref_seq_entries")
	}

	if len(upepRefSeqEntryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.GiUpepRefSeqEntries = resultSlice
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.GiID.Int64 {
				local.R.GiUpepRefSeqEntries = append(local.R.GiUpepRefSeqEntries, foreign)
				break
			}
		}
	}

	return nil
}

// AddGiUpepRefSeqEntriesG adds the given related objects to the existing relationships
// of the upep_gene_identifier, optionally inserting them as new records.
// Appends related to o.R.GiUpepRefSeqEntries.
// Sets related.R.Gi appropriately.
// Uses the global database handle.
func (o *UpepGeneIdentifier) AddGiUpepRefSeqEntriesG(insert bool, related ...*UpepRefSeqEntry) error {
	return o.AddGiUpepRefSeqEntries(boil.GetDB(), insert, related...)
}

// AddGiUpepRefSeqEntriesP adds the given related objects to the existing relationships
// of the upep_gene_identifier, optionally inserting them as new records.
// Appends related to o.R.GiUpepRefSeqEntries.
// Sets related.R.Gi appropriately.
// Panics on error.
func (o *UpepGeneIdentifier) AddGiUpepRefSeqEntriesP(exec boil.Executor, insert bool, related ...*UpepRefSeqEntry) {
	if err := o.AddGiUpepRefSeqEntries(exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddGiUpepRefSeqEntriesGP adds the given related objects to the existing relationships
// of the upep_gene_identifier, optionally inserting them as new records.
// Appends related to o.R.GiUpepRefSeqEntries.
// Sets related.R.Gi appropriately.
// Uses the global database handle and panics on error.
func (o *UpepGeneIdentifier) AddGiUpepRefSeqEntriesGP(insert bool, related ...*UpepRefSeqEntry) {
	if err := o.AddGiUpepRefSeqEntries(boil.GetDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddGiUpepRefSeqEntries adds the given related objects to the existing relationships
// of the upep_gene_identifier, optionally inserting them as new records.
// Appends related to o.R.GiUpepRefSeqEntries.
// Sets related.R.Gi appropriately.
func (o *UpepGeneIdentifier) AddGiUpepRefSeqEntries(exec boil.Executor, insert bool, related ...*UpepRefSeqEntry) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GiID.Int64 = o.ID
			rel.GiID.Valid = true
			if err = rel.Insert(exec); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"upep\".\"upep_ref_seq_entries\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"gi_id"}),
				strmangle.WhereClause("\"", "\"", 2, upepRefSeqEntryPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}

			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GiID.Int64 = o.ID
			rel.GiID.Valid = true
		}
	}

	if o.R == nil {
		o.R = &upepGeneIdentifierR{
			GiUpepRefSeqEntries: related,
		}
	} else {
		o.R.GiUpepRefSeqEntries = append(o.R.GiUpepRefSeqEntries, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &upepRefSeqEntryR{
				Gi: o,
			}
		} else {
			rel.R.Gi = o
		}
	}
	return nil
}

// SetGiUpepRefSeqEntriesG removes all previously related items of the
// upep_gene_identifier replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Gi's GiUpepRefSeqEntries accordingly.
// Replaces o.R.GiUpepRefSeqEntries with related.
// Sets related.R.Gi's GiUpepRefSeqEntries accordingly.
// Uses the global database handle.
func (o *UpepGeneIdentifier) SetGiUpepRefSeqEntriesG(insert bool, related ...*UpepRefSeqEntry) error {
	return o.SetGiUpepRefSeqEntries(boil.GetDB(), insert, related...)
}

// SetGiUpepRefSeqEntriesP removes all previously related items of the
// upep_gene_identifier replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Gi's GiUpepRefSeqEntries accordingly.
// Replaces o.R.GiUpepRefSeqEntries with related.
// Sets related.R.Gi's GiUpepRefSeqEntries accordingly.
// Panics on error.
func (o *UpepGeneIdentifier) SetGiUpepRefSeqEntriesP(exec boil.Executor, insert bool, related ...*UpepRefSeqEntry) {
	if err := o.SetGiUpepRefSeqEntries(exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetGiUpepRefSeqEntriesGP removes all previously related items of the
// upep_gene_identifier replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Gi's GiUpepRefSeqEntries accordingly.
// Replaces o.R.GiUpepRefSeqEntries with related.
// Sets related.R.Gi's GiUpepRefSeqEntries accordingly.
// Uses the global database handle and panics on error.
func (o *UpepGeneIdentifier) SetGiUpepRefSeqEntriesGP(insert bool, related ...*UpepRefSeqEntry) {
	if err := o.SetGiUpepRefSeqEntries(boil.GetDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetGiUpepRefSeqEntries removes all previously related items of the
// upep_gene_identifier replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Gi's GiUpepRefSeqEntries accordingly.
// Replaces o.R.GiUpepRefSeqEntries with related.
// Sets related.R.Gi's GiUpepRefSeqEntries accordingly.
func (o *UpepGeneIdentifier) SetGiUpepRefSeqEntries(exec boil.Executor, insert bool, related ...*UpepRefSeqEntry) error {
	query := "update \"upep\".\"upep_ref_seq_entries\" set \"gi_id\" = null where \"gi_id\" = $1"
	values := []interface{}{o.ID}
	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err := exec.Exec(query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.GiUpepRefSeqEntries {
			rel.GiID.Valid = false
			if rel.R == nil {
				continue
			}

			rel.R.Gi = nil
		}

		o.R.GiUpepRefSeqEntries = nil
	}
	return o.AddGiUpepRefSeqEntries(exec, insert, related...)
}

// RemoveGiUpepRefSeqEntriesG relationships from objects passed in.
// Removes related items from R.GiUpepRefSeqEntries (uses pointer comparison, removal does not keep order)
// Sets related.R.Gi.
// Uses the global database handle.
func (o *UpepGeneIdentifier) RemoveGiUpepRefSeqEntriesG(related ...*UpepRefSeqEntry) error {
	return o.RemoveGiUpepRefSeqEntries(boil.GetDB(), related...)
}

// RemoveGiUpepRefSeqEntriesP relationships from objects passed in.
// Removes related items from R.GiUpepRefSeqEntries (uses pointer comparison, removal does not keep order)
// Sets related.R.Gi.
// Panics on error.
func (o *UpepGeneIdentifier) RemoveGiUpepRefSeqEntriesP(exec boil.Executor, related ...*UpepRefSeqEntry) {
	if err := o.RemoveGiUpepRefSeqEntries(exec, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveGiUpepRefSeqEntriesGP relationships from objects passed in.
// Removes related items from R.GiUpepRefSeqEntries (uses pointer comparison, removal does not keep order)
// Sets related.R.Gi.
// Uses the global database handle and panics on error.
func (o *UpepGeneIdentifier) RemoveGiUpepRefSeqEntriesGP(related ...*UpepRefSeqEntry) {
	if err := o.RemoveGiUpepRefSeqEntries(boil.GetDB(), related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveGiUpepRefSeqEntries relationships from objects passed in.
// Removes related items from R.GiUpepRefSeqEntries (uses pointer comparison, removal does not keep order)
// Sets related.R.Gi.
func (o *UpepGeneIdentifier) RemoveGiUpepRefSeqEntries(exec boil.Executor, related ...*UpepRefSeqEntry) error {
	var err error
	for _, rel := range related {
		rel.GiID.Valid = false
		if rel.R != nil {
			rel.R.Gi = nil
		}
		if err = rel.Update(exec, "gi_id"); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.GiUpepRefSeqEntries {
			if rel != ri {
				continue
			}

			ln := len(o.R.GiUpepRefSeqEntries)
			if ln > 1 && i < ln-1 {
				o.R.GiUpepRefSeqEntries[i] = o.R.GiUpepRefSeqEntries[ln-1]
			}
			o.R.GiUpepRefSeqEntries = o.R.GiUpepRefSeqEntries[:ln-1]
			break
		}
	}

	return nil
}

// UpepGeneIdentifiersG retrieves all records.
func UpepGeneIdentifiersG(mods ...qm.QueryMod) upepGeneIdentifierQuery {
	return UpepGeneIdentifiers(boil.GetDB(), mods...)
}

// UpepGeneIdentifiers retrieves all the records using an executor.
func UpepGeneIdentifiers(exec boil.Executor, mods ...qm.QueryMod) upepGeneIdentifierQuery {
	mods = append(mods, qm.From("\"upep\".\"upep_gene_identifiers\""))
	return upepGeneIdentifierQuery{NewQuery(exec, mods...)}
}

// FindUpepGeneIdentifierG retrieves a single record by ID.
func FindUpepGeneIdentifierG(id int64, selectCols ...string) (*UpepGeneIdentifier, error) {
	return FindUpepGeneIdentifier(boil.GetDB(), id, selectCols...)
}

// FindUpepGeneIdentifierGP retrieves a single record by ID, and panics on error.
func FindUpepGeneIdentifierGP(id int64, selectCols ...string) *UpepGeneIdentifier {
	retobj, err := FindUpepGeneIdentifier(boil.GetDB(), id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindUpepGeneIdentifier retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUpepGeneIdentifier(exec boil.Executor, id int64, selectCols ...string) (*UpepGeneIdentifier, error) {
	upepGeneIdentifierObj := &UpepGeneIdentifier{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"upep\".\"upep_gene_identifiers\" where \"id\"=$1", sel,
	)

	q := queries.Raw(exec, query, id)

	err := q.Bind(upepGeneIdentifierObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from upep_gene_identifiers")
	}

	return upepGeneIdentifierObj, nil
}

// FindUpepGeneIdentifierP retrieves a single record by ID with an executor, and panics on error.
func FindUpepGeneIdentifierP(exec boil.Executor, id int64, selectCols ...string) *UpepGeneIdentifier {
	retobj, err := FindUpepGeneIdentifier(exec, id, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *UpepGeneIdentifier) InsertG(whitelist ...string) error {
	return o.Insert(boil.GetDB(), whitelist...)
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *UpepGeneIdentifier) InsertGP(whitelist ...string) {
	if err := o.Insert(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *UpepGeneIdentifier) InsertP(exec boil.Executor, whitelist ...string) {
	if err := o.Insert(exec, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// Whitelist behavior: If a whitelist is provided, only those columns supplied are inserted
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns without a default value are included (i.e. name, age)
// - All columns with a default, but non-zero are included (i.e. health = 75)
func (o *UpepGeneIdentifier) Insert(exec boil.Executor, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no upep_gene_identifiers provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.Time.IsZero() {
		o.CreatedAt.Time = currTime
		o.CreatedAt.Valid = true
	}
	if o.UpdatedAt.Time.IsZero() {
		o.UpdatedAt.Time = currTime
		o.UpdatedAt.Valid = true
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(upepGeneIdentifierColumnsWithDefault, o)

	key := makeCacheKey(whitelist, nzDefaults)
	upepGeneIdentifierInsertCacheMut.RLock()
	cache, cached := upepGeneIdentifierInsertCache[key]
	upepGeneIdentifierInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := strmangle.InsertColumnSet(
			upepGeneIdentifierColumns,
			upepGeneIdentifierColumnsWithDefault,
			upepGeneIdentifierColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		cache.valueMapping, err = queries.BindMapping(upepGeneIdentifierType, upepGeneIdentifierMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(upepGeneIdentifierType, upepGeneIdentifierMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"upep\".\"upep_gene_identifiers\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.IndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"upep\".\"upep_gene_identifiers\" DEFAULT VALUES"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		if len(wl) != 0 {
			cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into upep_gene_identifiers")
	}

	if !cached {
		upepGeneIdentifierInsertCacheMut.Lock()
		upepGeneIdentifierInsertCache[key] = cache
		upepGeneIdentifierInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// UpdateG a single UpepGeneIdentifier record. See Update for
// whitelist behavior description.
func (o *UpepGeneIdentifier) UpdateG(whitelist ...string) error {
	return o.Update(boil.GetDB(), whitelist...)
}

// UpdateGP a single UpepGeneIdentifier record.
// UpdateGP takes a whitelist of column names that should be updated.
// Panics on error. See Update for whitelist behavior description.
func (o *UpepGeneIdentifier) UpdateGP(whitelist ...string) {
	if err := o.Update(boil.GetDB(), whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateP uses an executor to update the UpepGeneIdentifier, and panics on error.
// See Update for whitelist behavior description.
func (o *UpepGeneIdentifier) UpdateP(exec boil.Executor, whitelist ...string) {
	err := o.Update(exec, whitelist...)
	if err != nil {
		panic(boil.WrapErr(err))
	}
}

// Update uses an executor to update the UpepGeneIdentifier.
// Whitelist behavior: If a whitelist is provided, only the columns given are updated.
// No whitelist behavior: Without a whitelist, columns are inferred by the following rules:
// - All columns are inferred to start with
// - All primary keys are subtracted from this set
// Update does not automatically update the record in case of default values. Use .Reload()
// to refresh the records.
func (o *UpepGeneIdentifier) Update(exec boil.Executor, whitelist ...string) error {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt.Time = currTime
	o.UpdatedAt.Valid = true

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return err
	}
	key := makeCacheKey(whitelist, nil)
	upepGeneIdentifierUpdateCacheMut.RLock()
	cache, cached := upepGeneIdentifierUpdateCache[key]
	upepGeneIdentifierUpdateCacheMut.RUnlock()

	if !cached {
		wl := strmangle.UpdateColumnSet(
			upepGeneIdentifierColumns,
			upepGeneIdentifierPrimaryKeyColumns,
			whitelist,
		)

		if len(whitelist) == 0 {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return errors.New("models: unable to update upep_gene_identifiers, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"upep\".\"upep_gene_identifiers\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, upepGeneIdentifierPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(upepGeneIdentifierType, upepGeneIdentifierMapping, append(wl, upepGeneIdentifierPrimaryKeyColumns...))
		if err != nil {
			return err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	_, err = exec.Exec(cache.query, values...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update upep_gene_identifiers row")
	}

	if !cached {
		upepGeneIdentifierUpdateCacheMut.Lock()
		upepGeneIdentifierUpdateCache[key] = cache
		upepGeneIdentifierUpdateCacheMut.Unlock()
	}

	return o.doAfterUpdateHooks(exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q upepGeneIdentifierQuery) UpdateAllP(cols M) {
	if err := q.UpdateAll(cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values.
func (q upepGeneIdentifierQuery) UpdateAll(cols M) error {
	queries.SetUpdate(q.Query, cols)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to update all for upep_gene_identifiers")
	}

	return nil
}

// UpdateAllG updates all rows with the specified column values.
func (o UpepGeneIdentifierSlice) UpdateAllG(cols M) error {
	return o.UpdateAll(boil.GetDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o UpepGeneIdentifierSlice) UpdateAllGP(cols M) {
	if err := o.UpdateAll(boil.GetDB(), cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o UpepGeneIdentifierSlice) UpdateAllP(exec boil.Executor, cols M) {
	if err := o.UpdateAll(exec, cols); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UpepGeneIdentifierSlice) UpdateAll(exec boil.Executor, cols M) error {
	ln := int64(len(o))
	if ln == 0 {
		return nil
	}

	if len(cols) == 0 {
		return errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), upepGeneIdentifierPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"upep\".\"upep_gene_identifiers\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, upepGeneIdentifierPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to update all in upepGeneIdentifier slice")
	}

	return nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *UpepGeneIdentifier) UpsertG(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	return o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *UpepGeneIdentifier) UpsertGP(updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(boil.GetDB(), updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *UpepGeneIdentifier) UpsertP(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) {
	if err := o.Upsert(exec, updateOnConflict, conflictColumns, updateColumns, whitelist...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
func (o *UpepGeneIdentifier) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns []string, whitelist ...string) error {
	if o == nil {
		return errors.New("models: no upep_gene_identifiers provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.Time.IsZero() {
		o.CreatedAt.Time = currTime
		o.CreatedAt.Valid = true
	}
	o.UpdatedAt.Time = currTime
	o.UpdatedAt.Valid = true

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(upepGeneIdentifierColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs postgres problems
	buf := strmangle.GetBuffer()

	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range updateColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range whitelist {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	upepGeneIdentifierUpsertCacheMut.RLock()
	cache, cached := upepGeneIdentifierUpsertCache[key]
	upepGeneIdentifierUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := strmangle.InsertColumnSet(
			upepGeneIdentifierColumns,
			upepGeneIdentifierColumnsWithDefault,
			upepGeneIdentifierColumnsWithoutDefault,
			nzDefaults,
			whitelist,
		)

		update := strmangle.UpdateColumnSet(
			upepGeneIdentifierColumns,
			upepGeneIdentifierPrimaryKeyColumns,
			updateColumns,
		)
		if len(update) == 0 {
			return errors.New("models: unable to upsert upep_gene_identifiers, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(upepGeneIdentifierPrimaryKeyColumns))
			copy(conflict, upepGeneIdentifierPrimaryKeyColumns)
		}
		cache.query = queries.BuildUpsertQueryPostgres(dialect, "\"upep\".\"upep_gene_identifiers\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(upepGeneIdentifierType, upepGeneIdentifierMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(upepGeneIdentifierType, upepGeneIdentifierMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert upep_gene_identifiers")
	}

	if !cached {
		upepGeneIdentifierUpsertCacheMut.Lock()
		upepGeneIdentifierUpsertCache[key] = cache
		upepGeneIdentifierUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// DeleteP deletes a single UpepGeneIdentifier record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *UpepGeneIdentifier) DeleteP(exec boil.Executor) {
	if err := o.Delete(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteG deletes a single UpepGeneIdentifier record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *UpepGeneIdentifier) DeleteG() error {
	if o == nil {
		return errors.New("models: no UpepGeneIdentifier provided for deletion")
	}

	return o.Delete(boil.GetDB())
}

// DeleteGP deletes a single UpepGeneIdentifier record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *UpepGeneIdentifier) DeleteGP() {
	if err := o.DeleteG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Delete deletes a single UpepGeneIdentifier record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UpepGeneIdentifier) Delete(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no UpepGeneIdentifier provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), upepGeneIdentifierPrimaryKeyMapping)
	sql := "DELETE FROM \"upep\".\"upep_gene_identifiers\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete from upep_gene_identifiers")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return err
	}

	return nil
}

// DeleteAllP deletes all rows, and panics on error.
func (q upepGeneIdentifierQuery) DeleteAllP() {
	if err := q.DeleteAll(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all matching rows.
func (q upepGeneIdentifierQuery) DeleteAll() error {
	if q.Query == nil {
		return errors.New("models: no upepGeneIdentifierQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	_, err := q.Query.Exec()
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from upep_gene_identifiers")
	}

	return nil
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o UpepGeneIdentifierSlice) DeleteAllGP() {
	if err := o.DeleteAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAllG deletes all rows in the slice.
func (o UpepGeneIdentifierSlice) DeleteAllG() error {
	if o == nil {
		return errors.New("models: no UpepGeneIdentifier slice provided for delete all")
	}
	return o.DeleteAll(boil.GetDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o UpepGeneIdentifierSlice) DeleteAllP(exec boil.Executor) {
	if err := o.DeleteAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UpepGeneIdentifierSlice) DeleteAll(exec boil.Executor) error {
	if o == nil {
		return errors.New("models: no UpepGeneIdentifier slice provided for delete all")
	}

	if len(o) == 0 {
		return nil
	}

	if len(upepGeneIdentifierBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), upepGeneIdentifierPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"upep\".\"upep_gene_identifiers\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, upepGeneIdentifierPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	_, err := exec.Exec(sql, args...)
	if err != nil {
		return errors.Wrap(err, "models: unable to delete all from upepGeneIdentifier slice")
	}

	if len(upepGeneIdentifierAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return err
			}
		}
	}

	return nil
}

// ReloadGP refetches the object from the database and panics on error.
func (o *UpepGeneIdentifier) ReloadGP() {
	if err := o.ReloadG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *UpepGeneIdentifier) ReloadP(exec boil.Executor) {
	if err := o.Reload(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadG refetches the object from the database using the primary keys.
func (o *UpepGeneIdentifier) ReloadG() error {
	if o == nil {
		return errors.New("models: no UpepGeneIdentifier provided for reload")
	}

	return o.Reload(boil.GetDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UpepGeneIdentifier) Reload(exec boil.Executor) error {
	ret, err := FindUpepGeneIdentifier(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *UpepGeneIdentifierSlice) ReloadAllGP() {
	if err := o.ReloadAllG(); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *UpepGeneIdentifierSlice) ReloadAllP(exec boil.Executor) {
	if err := o.ReloadAll(exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UpepGeneIdentifierSlice) ReloadAllG() error {
	if o == nil {
		return errors.New("models: empty UpepGeneIdentifierSlice provided for reload all")
	}

	return o.ReloadAll(boil.GetDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UpepGeneIdentifierSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	upepGeneIdentifiers := UpepGeneIdentifierSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), upepGeneIdentifierPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"upep\".\"upep_gene_identifiers\".* FROM \"upep\".\"upep_gene_identifiers\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, upepGeneIdentifierPrimaryKeyColumns, len(*o))

	q := queries.Raw(exec, sql, args...)

	err := q.Bind(&upepGeneIdentifiers)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UpepGeneIdentifierSlice")
	}

	*o = upepGeneIdentifiers

	return nil
}

// UpepGeneIdentifierExists checks if the UpepGeneIdentifier row exists.
func UpepGeneIdentifierExists(exec boil.Executor, id int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"upep\".\"upep_gene_identifiers\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, id)
	}

	row := exec.QueryRow(sql, id)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if upep_gene_identifiers exists")
	}

	return exists, nil
}

// UpepGeneIdentifierExistsG checks if the UpepGeneIdentifier row exists.
func UpepGeneIdentifierExistsG(id int64) (bool, error) {
	return UpepGeneIdentifierExists(boil.GetDB(), id)
}

// UpepGeneIdentifierExistsGP checks if the UpepGeneIdentifier row exists. Panics on error.
func UpepGeneIdentifierExistsGP(id int64) bool {
	e, err := UpepGeneIdentifierExists(boil.GetDB(), id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// UpepGeneIdentifierExistsP checks if the UpepGeneIdentifier row exists. Panics on error.
func UpepGeneIdentifierExistsP(exec boil.Executor, id int64) bool {
	e, err := UpepGeneIdentifierExists(exec, id)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}
